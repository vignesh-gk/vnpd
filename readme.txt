
The model is already trained using train20x20 dataset which contains 20x20 croped numbers and letters.
'trainRecognizeCharacters.py' is used  to train the mode.

already we have downloaded the dataset using ' python downloadDataset.py '

run  :  python charpredict.py 

It will predict the characters of the numberplate present in the cars.

The images of the cars are present in the './datasets/' folder